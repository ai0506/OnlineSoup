# OnlineSoup（汤局）— 前端界面规格

本文件定义 OnlineSoup 后续前端开发应遵循的体验边界，适用于大厅、房间、个人资料、反馈和管理后台。业务规则、权限与数据结构仍以 `AGENTS.md`、迁移和当前代码为准。

## 1. 视觉方向

- 保持当前的中性纸白、深蓝文字、细边框和小圆角风格；界面以清晰阅读和多人协作为优先，不追求夸张动效或装饰。
- 普通页面保持轻量、居中、留白充分；房间页让聊天内容成为第一视觉焦点。
- 站点支持系统浅色与深色模式。新增颜色必须同时检查两种模式，不能只在浅色模式可读。
- 玩家可见文案只描述任务和结果，不显示迁移名、内部状态、数据库或模型调试信息。

## 2. 核心原则：界面稳定，杜绝抖动

> 这是全站所有前端页面和组件的共同要求。切换选项、标签、筛选、展开状态或异步状态时，不得让整个界面发生明显变形、位移或滚动跳转。

- 固定区域（站点头、页面标题、主要按钮、筛选条件、标签栏、房间码与题目摘要、聊天输入、积分摘要）的位置和尺寸应稳定；内容增减不能让它们跳动。
- 切换标签、筛选、分页、视图、折叠面板、表单选项或加载状态时，只更新需要变化的内容区。标签栏、标题、操作按钮和相邻区域必须维持原有位置，不能因为内容长短不同大幅移动。
- 为标题、计数、状态文案、按钮、错误提示和列表预留合理的稳定空间；长文字应换行、截断或在内部滚动，不能把主要布局撑开或推走其他控件。
- 空态、加载态、错误态和有内容状态应占用相近的容器尺寸。摘要列表必须使用固定高度或最小高度，避免记录数量变化导致卡片忽大忽小。
- 房间页使用固定视口外壳：外层不滚动，消息列表和房间详情各自在内层滚动。不要让滚动在外层与内层之间切换；需要时使用 `scrollbar-gutter: stable` 防止横向位移。
- 其他带长内容的页面也应明确唯一滚动容器；不得在状态切换后让滚动从页面外层改到卡片内层，或让滚动条出现/消失带动内容横移。
- 新消息到达只更新消息区；不要重建整个房间页、强制重置滚动位置，或打断正在输入的文本。
- 自动刷新不得在输入框聚焦、弹窗打开或管理员正在筛选/编辑时覆盖用户操作。
- 临时提示使用既有浮层或固定预留区域，不能把聊天输入框、表单或卡片向下挤开。积分不足、发送过快、AI 失败等提示不得堆在聊天输入框附近。
- 弹窗中因开关、校验或异步状态变化而暂时不可用的字段，应保留布局位置；错误文案预留空间，避免弹窗高度跳动。
- 当列表只展示摘要时，容器高度应固定。例如个人资料页的积分流水预览只显示部分记录，记录增减不能改变卡片高度；完整内容放到独立页面或弹层。

## 3. 信息层级与复杂操作

- 房间页优先显示聊天与房间码。座位、临时积分、房主操作、题目管理和事实面板属于房间详情，不与聊天主区争夺空间。
- 新功能不要直接塞进主界面。复杂信息或低频操作优先通过按钮进入弹窗、抽屉、折叠面板、二级标签或独立详情页。
- 具有破坏性或不可逆后果的操作（如退出、踢人、关闭房间、删除）必须使用站内样式一致的二次确认，不能使用浏览器原生 `alert`、`confirm` 或 `prompt`。
- 所有状态都要有明确的空态、加载态或失败态；失败只说明玩家可执行的下一步，不暴露内部原因。

## 4. 布局与响应式

- 常规页面使用当前居中内容宽度（最大约 1100px），卡片和表单在窄屏改为单列，保持足够的触控间距。
- 房间页优先按照可用空间组织，而不是只依据设备名称：
  - 宽阔横屏（当前实现为至少 880px 且宽高比足够大）使用聊天主区加右侧约 300px 的房间详情栏。
  - 宽度不超过 760px 的窄屏使用纵向布局；横向标签内容可内部横向滚动，但页面整体不能横向溢出。
  - 竖屏手机使用常驻房间信息栏和「聊天 / 题目 / 座位」三段式导航。聊天段保持输入框可用；题目和座位段独立填满可用空间并在其内部滚动。
- 切换手机房间分段时，保留每个分段的必要状态；不应因切换造成聊天草稿丢失、跳回页面顶部或整体布局重排。
- 顶部图标和菜单必须保留可见提示、`aria-label`、键盘焦点态和至少 44×44px 的触控区域；窄屏可隐藏非关键文字，但不能让操作变得不可理解。

## 5. 房间聊天与 AI 交互

- 聊天输入区始终位于消息区底部，四种模式（聊天、询问、提示、尝试推理）切换不能改变输入区的主位置或显著挤压消息区。
- AI 请求进行中应只锁定本次必要操作，并给出清楚、就地的状态；不能让其他房间信息闪烁或被清空。
- Realtime 用于及时更新，但页面恢复、订阅断开或事件遗漏后必须通过受保护接口补拉真实状态；界面不能把临时事件当作唯一事实来源。
- 题目、事实、提示和推理结果必须按当前题目隔离展示。玩家侧不得展示 `ask_audit`、`reason`、缓存命中或其他调试字段。
- 被踢、房间关闭、其他设备接管或会话失效时，以明确的只读/跳转提示结束当前操作；不要留下看似仍可发送但必然失败的输入状态。

## 6. 表单、列表与积分

- 表单提供合理默认值，前端校验与服务端校验一致；校验提示使用明确中文，不因显示/隐藏而改变主要按钮位置。
- 长列表（聊天、积分流水、后台表格）应分页、分段或内部滚动，避免无限拉长主页面。
- 积分余额、消耗与退款使用统一且易区分的正负语义颜色，同时必须保留文字和数值，不能只依赖颜色。
- 个人资料、反馈和管理后台属于独立任务页面，不能影响正在进行的房间会话。
- 管理端自动刷新不能覆盖正在填写的输入、打开的弹窗或当前筛选条件。

## 7. 交互、动效与可访问性

- 所有可点击按钮、标签和图标在 hover、active、focus-visible 状态下都应有克制且即时的反馈；不使用弹跳、长时间或会造成注意力干扰的动效。
- 尊重 `prefers-reduced-motion`；不得用持续动画传达唯一信息。
- 可频繁点击的控件文字默认不可选中（`user-select: none`），但聊天正文、题面、房间码和错误说明必须可选择、复制或通过已有复制按钮取得。
- 颜色、图标和位置只能作为辅助提示。状态、错误、积分变动和操作结果必须有可读文本。
- 弹窗、抽屉和展开菜单需要可用键盘操作；打开后焦点进入组件，关闭后回到触发控件，Escape 和遮罩关闭行为须与现有组件保持一致。

## 8. 实施与验收

- 修改前先查当前组件、`globals.css` 的响应式规则和相关业务约束；不要因为视觉需求破坏房间成员校验、Cookie、积分事务或 Realtime 恢复。
- 视觉改动后至少检查：桌面横屏、手机竖屏、深浅色模式、长文本、空态、加载态、错误态、键盘焦点和正在输入聊天时的刷新行为。
- 涉及页面、路由或交互组件时运行 `npm run typecheck`、`npm run lint` 和 `npm run build`；房间相关改动还应以登录用户、游客、刷新恢复和并发状态做实际流程验证。

## 9. 规范优先级

发生冲突时，按以下顺序判断：

1. 当前代码、已部署数据库和真实行为。
2. `AGENTS.md` 中的安全、房间、聊天、积分与管理端约束。
3. 本前端界面规格。
4. 早期架构设计和静态原型。
